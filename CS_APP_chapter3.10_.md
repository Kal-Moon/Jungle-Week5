# 컴퓨터 시스템 3.10.5 — 가변 크기 스택 프레임 지원하기

지금까지 3장에서 본 함수들은 전부 한 가지 공통점이 있었습니다. **스택 프레임의 크기를 컴파일러가 미리 알 수 있었다**는 점입니다. 그래서 함수 시작부가 항상 `subq $16, %rsp` 처럼 **상수**를 빼는 형태였습니다.

이번 절은 그 전제가 깨지는 경우를 다룹니다.

---

## 1. 크기를 미리 알 수 없는 함수

프레임 크기를 컴파일 시점에 정할 수 없는 경우는 두 가지입니다.

**① `alloca` 호출**

라이브러리 함수입니다. `malloc`처럼 원하는 바이트 수를 받아 공간을 주는데, 주는 위치가 힙이 아니라 **지금 실행 중인 함수의 스택 프레임 안**입니다. 그래서 함수가 리턴하면 프레임과 함께 자동으로 사라집니다. 해제 함수가 따로 없는 이유입니다.

**② 가변 크기 지역배열 선언**

이건 함수 호출이 아니라 **언어 문법**입니다. ISO C99부터 배열 크기 칸에 상수 대신 변수를 쓸 수 있게 되었습니다.

```c
long vframe(long n, long idx, long *q) {
    long i;
    long *p[n];          /* ← 크기가 런타임에 결정됨 (8n 바이트) */
    p[0] = &i;
    for (i = 1; i < n; i++)
        p[i] = q;
    return *p[idx];
}
```

> 💡 둘은 **서로 다른 경로**입니다. 하나는 함수를 부르는 동작이고, 하나는 선언이라는 문법입니다. 다만 컴파일러 입장에서는 "프레임에 실행 시점 크기의 공간을 잡아야 한다"는 **같은 숙제**가 됩니다.

핵심은 호출 횟수가 아니라 **호출될 때마다 `n`이 다를 수 있다는 것**입니다. 코드는 하나인데 대응해야 하는 크기는 여러 가지라서, 상수를 못 박을 수가 없습니다.

---

## 2. 무엇이 정확히 문제인가

전제 두 가지를 먼저 깔겠습니다.

- 스택은 **낮은 주소 쪽으로** 자랍니다. 공간을 더 쓰려면 `%rsp`에서 **뺍니다.**
- 기계어에는 `i` 같은 이름이 없습니다. 스택 변수는 오직 **기준 레지스터 + 상수 오프셋**으로만 지목할 수 있고, 그 상수는 컴파일할 때 확정되어 기계어에 박힙니다.

함수에 들어온 순간 `%rsp`가 1000이라고 두고, `n=5`일 때를 따라가 보겠습니다.

| 단계 | %rsp | i의 주소 | %rsp에서 i까지 거리 |
|---|---|---|---|
| 진입 | 1000 | — | — |
| i 공간 확보 (16) | 984 | **984** | 0 |
| p 공간 확보 (8×5=40) | 944 | 984 | **40** |

같은 함수를 `n=100`으로 부르면:

| 단계 | %rsp | i의 주소 | %rsp에서 i까지 거리 |
|---|---|---|---|
| 진입 | 1000 | — | — |
| i 공간 확보 (16) | 984 | **984** | 0 |
| p 공간 확보 (8×100=800) | 184 | 984 | **800** |

```
        n = 5                      n = 100
1000 ─ 리턴주소                1000 ─ 리턴주소
 984 ─ i        ← 여기까진      984 ─ i        ← 똑같음
      ┌────────┐                    ┌────────┐
      │ p (40) │                    │        │
 944 ─└────────┘ %rsp               │ p(800) │
                                    │        │
 184 ─────────────────────────      └────────┘ %rsp
```

`i`를 부르는 명령은 기계어에 **딱 하나**만 존재합니다. 그런데 그 하나가 어떨 땐 `40(%rsp)`여야 하고 어떨 땐 `800(%rsp)`여야 합니다. 컴파일러는 어느 쪽을 박아야 할지 정할 수 없습니다.

> 💡 그런데 표를 다시 보면, **`i`의 주소 984는 양쪽 다 똑같습니다.** `i`의 자리는 `p`를 확보하기 **전에** 이미 정해졌기 때문입니다. 흔들리는 건 `%rsp`뿐이고, 문제는 `%rsp`가 그 자리를 잊어버린다는 것입니다.

필요한 건 하나입니다 — **`p`를 확보하기 직전의 위치를 기억하는 별도의 레지스터.**

---

## 3. 그런데 왜 `i`가 스택에 있는가

여기서 잠깐. 지역변수는 보통 레지스터에 두는 게 빠른데, 왜 `i`가 스택으로 내려갔을까요.

`p[0] = &i` 때문입니다.

**레지스터에는 주소가 없습니다.** `%rax`는 CPU 칩 안에 있는 저장소이고, 번지가 붙어 있는 건 메모리뿐입니다. 포인터는 "몇 번지"라는 숫자를 담는 변수인데, 레지스터에는 담을 숫자 자체가 존재하지 않습니다.

```
CPU 칩 안                        메모리 (RAM)
┌──────────────┐                ┌──────────────┐
│ %rax  %rbx   │                │ 0번지        │
│ %rcx  %rdx   │   ←── 버스 ──→ │ 1번지        │
│ %rsp  %rbp   │                │ 2번지        │
└──────────────┘                └──────────────┘
 이름으로만 지목           번호(주소)로 지목
```

그래서 컴파일러의 판단은 이렇게 흘러갑니다.

1. 지역변수 `i`를 본다 → 레지스터에 두고 싶다
2. `&i`를 본다 → 주소를 내놔야 한다
3. 레지스터에는 주소가 없다 → **메모리에 자리를 줘야 한다**

`&`를 쓰는 순간 그 변수는 빠른 쪽을 포기하고 주소가 있는 쪽으로 내려갑니다.

---

## 4. 해결책 — 프레임 포인터

x86-64는 이 문제를 `%rbp`를 **프레임 포인터**(base pointer — 그래서 이름이 `bp`)로 세워 해결합니다.

| | %rsp | %rbp |
|---|---|---|
| 역할 | 스택의 현재 끝 | 이 프레임의 기준점 |
| 함수 실행 중 | 배열 할당으로 **움직인다** | **고정** |
| 무엇을 재나 | 가변 영역(`p`) | 고정 영역(`i`) |

`%rbp`를 기준으로 재면 `n`이 무엇이든 거리가 같습니다.

| n | %rbp | i의 주소 | 오프셋 |
|---|---|---|---|
| 5 | 992 | 984 | **−8** |
| 100 | 992 | 984 | **−8** |

> 💡 `%rbp`는 특별한 하드웨어 기능이 있는 레지스터가 아닙니다. 범용 레지스터 16개 중 하나를 **관례상** 이 역할에 쓰는 것뿐입니다. 이름의 `bp`가 그 관례의 흔적입니다. `%rsp`도 마찬가지로 `sp` = stack pointer입니다.

---

## 5. vframe 코드 따라 읽기

GCC가 만든 코드를 순서대로 보겠습니다. 진입 시 `%rsp = 1000`(1000번지에 리턴주소)으로 가정합니다.

### 프롤로그 (2~3번 줄)

```
2  pushq %rbp          # 호출자의 %rbp를 스택에 치워둠
3  movq %rsp, %rbp     # 지금 위치를 내 기준점으로
```

| 실행 후 | %rsp | %rbp |
|---|---|---|
| 진입 | 1000 | 호출자의 값 |
| `pushq %rbp` | 992 | 호출자의 값 (992번지에 저장됨) |
| `movq %rsp, %rbp` | 992 | **992** |

`%rbp`는 **피호출자 저장(callee-saved) 레지스터**입니다. 쓰려면 원래 값을 치워두고, 돌아가기 전에 반드시 되돌려놔야 합니다. 그래서 순서가 강제됩니다 — 먼저 치우고(`pushq`), 그 다음 세웁니다(`movq`). 반대로 하면 옛 값이 이미 덮여 사라집니다.

### 고정 영역 확보 (4번 줄)

```
4  subq $16, %rsp
```

```
992 ─ 저장된 %rbp        ← %rbp
      ┌──────────────┐
984 ─ │ i            │   ← −8(%rbp)
      ├──────────────┤
976 ─ │ 사용 안 함    │   ← −16(%rbp)
      └──────────────┘   ← %rsp
```

`i`는 `long` 하나라 8바이트면 충분한데 16을 뺍니다. **스택 포인터를 16의 배수로 유지하는 관례** 때문입니다. 8만 빼면 그 관례가 깨집니다. 나머지 8바이트는 그냥 버려집니다.

> 💡 낭비가 실수가 아니라 관례의 결과라는 점. 저자가 "두 번째 8바이트는 사용하지 않는다"고 굳이 명시한 이유도, 독자가 `-16(%rbp)`에서 무언가를 찾아 헤매지 않게 하려는 것입니다.

### 가변 영역 확보 (5~11번 줄)

```
5  leaq 22(,%rdi,8), %rax    ┐
6  andq $-16, %rax           ├ 배열 p를 위한 공간 계산 및 할당
7  subq %rax, %rsp           ┘  (16의 배수로 반올림)
8  leaq 7(%rsp), %rax        ┐
9  shrq $3, %rax             ├ p의 시작 위치를 8의 배수로 맞춤
10 leaq 0(,%rax,8), %r8      ┘  &p[0]
11 movq %r8, %rcx
```

정렬 보정의 세부 계산은 저자가 **연습문제 3.49**로 넘겼습니다. 본문에서는 11번 줄 시점에 이것만 보장되면 충분하다고 말합니다.

- 스택에 **최소** `8n` 바이트를 할당했다
- 그 영역 안에 배열 `p`를 배치했다
- `p`의 시작주소가 `%rcx`에 들어 있다

`정확히`가 아니라 `최소`인 점에 주의. 정렬을 맞추느라 앞뒤로 여유 공간이 생깁니다.

### 루프 (12~19번 줄)

```
  i in %rax and on stack, n in %rdi, p in %rcx, q in %rdx
12 .L3:
13   movq %rdx, (%rcx,%rax,8)  # p[i] = q
14   addq $1, %rax             # i++
15   movq %rax, -8(%rbp)       # i를 스택에 저장   ← %rbp 기준!
16 .L2:
17   movq -8(%rbp), %rax       # i를 스택에서 읽음 ← %rbp 기준!
18   cmpq %rdi, %rax
19   jl .L3
```

여기서 이 절의 결론이 실물로 확인됩니다.

| 접근 대상 | 명령 | 기준 |
|---|---|---|
| `p[i]` | `movq %rdx, (%rcx,%rax,8)` | `%rcx` (실행 시점 계산값) |
| `i` | `movq %rax, -8(%rbp)` | `%rbp` (고정 기준점) |

`p`에 접근하는 13번 줄에는 `%rbp`도 `%rsp`도 나오지 않습니다. 가변 크기라는 사정이 5~11번 줄에서 전부 흡수되고, 실제 사용 단계는 고정 크기 배열과 똑같이 생겼습니다.

> 💡 `i`가 레지스터(`%rax`)와 스택에 **동시에** 존재하는 게 낭비처럼 보입니다. 하지만 `p[0]`에 `i`의 주소가 들어 있으니, 누군가 그 주소로 읽을 수 있습니다. 그래서 스택 사본이 항상 최신이어야 합니다. 실제로 마지막 줄 `return *p[idx]`에서 `idx`가 0이면 그 경로가 실행됩니다.

### 에필로그 (20~21번 줄)

```
20 leave
21 ret
```

`leave`는 인자가 없고, 다음 두 줄과 동일합니다.

```
movq %rbp, %rsp    # 스택 포인터를 프레임 시작으로
popq %rbp          # 저장된 %rbp 복원 + 스택 포인터를 호출자 프레임 끝으로
```

`n=100`으로 실행 중이라면:

| 실행 후 | %rsp | %rbp |
|---|---|---|
| 루프 종료 | 184 | 992 |
| `movq %rbp, %rsp` | **992** | 992 |
| `popq %rbp` | **1000** | 호출자의 값 |
| `ret` | 1008 | 호출자의 값 |

첫 줄이 이 절의 마지막 문제를 풉니다.

```
불가능한 방법:  addq $???, %rsp      ← ???를 알 수 없음
실제 방법:      movq %rbp, %rsp      ← 목표 위치를 통째로 덮어씀
```

**뺀 양을 몰라도 도착지를 알면 됩니다.** 이 한 줄로 `i` 영역, 정렬 여백, `p` 영역이 한꺼번에 반환됩니다.

프롤로그와 에필로그가 정확히 대칭을 이룹니다.

```
프롤로그:  pushq %rbp      →  movq %rsp, %rbp
에필로그:  movq %rbp, %rsp →  popq %rbp
```

---

## 6. 왜 요즘은 잘 안 쓰는가

초기 x86(IA32)에서는 프레임 포인터를 **매 함수 호출마다** 썼습니다. 레지스터가 8개뿐이었고 인자를 스택으로 전달했으니, `%ebp` 하나를 세워두면 인자와 지역변수를 전부 그 기준으로 정리할 수 있었습니다.

```
 8(%ebp)  ← 첫 번째 인자
 4(%ebp)  ← 리턴주소
 0(%ebp)  ← 저장된 %ebp
-4(%ebp)  ← 첫 번째 지역변수
```

x86-64는 사정이 달라졌습니다. 레지스터가 16개로 늘었고, **인자 6개까지 레지스터로** 전달합니다. 스택을 참조할 일 자체가 줄어서, `%rsp` 기준 오프셋만으로 대부분 처리됩니다. 그래서 최신 GCC는 이 관습을 버리고, **스택 프레임이 가변 크기일 가능성이 있을 때만** 씁니다.

> 💡 여기서 **"스택 프레임"과 "프레임 포인터"를 구분**해야 합니다. 스택 프레임은 지금도 모든 함수 호출마다 만들어집니다. 안 쓰게 된 건 `%rbp`를 기준점으로 세우는 관습 쪽입니다.

부작용도 있습니다. 프레임 포인터 사슬이 없으면 디버거가 호출 스택을 되짚기 어려워집니다. 그래서 컴파일러들은 실행 파일 안의 별도 섹션(`.eh_frame`, `.debug_frame`)에 **CFI**라는 대응표를 심어 해결했습니다. "이 명령 위치에서는 프레임이 `%rsp + 48`부터 시작한다"는 식의 표입니다. 실행 중에 레지스터를 소모하던 것을, 실행에 쓰이지 않는 정적 데이터로 옮긴 셈입니다.

그리고 마지막 문장이 중요합니다.

> 모든 함수들이 `%rbp`를 피호출자 저장 레지스터로 취급하는 한, 프레임 포인터를 사용하는 코드와 그렇지 않은 코드를 **혼합해서 사용하는 것이 허용**된다.

호출자 입장에서는 돌아왔을 때 `%rbp`가 그대로면 됩니다. 그 함수가 안에서 프레임 포인터로 썼든, 임시 저장소로 썼든, 아예 안 건드렸든 **관찰되는 결과가 같습니다.** 관례만 지키면 구현이 달라도 조립된다는 것 — 3장 전체를 관통하는 주제입니다.

---

## 7. 예제를 다시 보기

`vframe`은 계산상 아무 의미가 없는 함수입니다. 그런데 버릴 줄이 하나도 없습니다.

| 코드 요소 | 만들어내는 조건 |
|---|---|
| `long *p[n]` | 프레임 크기가 실행 시점에 결정됨 |
| 원소 타입이 포인터(8바이트) | 정렬 보정 코드가 필요해짐 |
| `p[0] = &i` | `i`가 스택으로 내려가 `%rbp` 오프셋으로 참조됨 |
| `for` 루프 | `i`와 `p` 양쪽에 반복 접근 — 두 방식이 대비됨 |
| `return *p[idx]` | 저장된 주소가 실제로 사용됨 |

특히 `p[0] = &i`가 눈여겨볼 장치입니다. 이 줄이 없어도 `%rbp`는 여전히 필요합니다(종료 시 `%rsp`를 되돌려야 하니까). 하지만 코드 어디에도 `-8(%rbp)`가 나타나지 않게 됩니다. **저자가 가르치려는 문장을 눈에 보이게 만들려고** 넣은 줄입니다.

> 💡 교과서 예제를 읽을 때 "이 줄이 없으면 무엇이 안 보이게 되는가"를 물어보면, 저자가 무엇을 보여주려 했는지가 대체로 드러납니다.

---

## 8. 정리

```
문제:  프레임 크기를 컴파일 시점에 모르는 함수가 있다
       (원인 둘 — alloca 호출 / 가변 크기 지역배열 선언)
          ↓
결과:  %rsp가 실행 시점에 얼마나 내려갈지 모른다
       → 고정 지역변수를 %rsp 기준 오프셋으로 적을 수 없다
          ↓
해결:  %rbp를 프레임 포인터로 고정 세우기
       · i  → -8(%rbp)            (고정 기준점)
       · p  → %rcx에 담은 시작주소 (실행 시점 계산)
       · 종료 → leave가 %rbp 값을 %rsp에 복사해 한 번에 반환
          ↓
관례:  IA32는 항상 사용 → x86-64는 필요할 때만
       %rbp를 피호출자 저장으로 취급하면 혼용 가능
```

이 절에서 건질 발상을 한 줄로 줄이면 이렇습니다.

**컴파일 시점에 모르는 값이 있으면, 실행 시점에 알 수 있는 기준점을 하나 잡아라.**

`leave`가 "뺀 양을 더하지 않고 목표 지점을 복사한다"로 문제를 푼 것도 같은 성격의 사고입니다. 크기를 모를 때 크기를 구하려 애쓰는 대신, 크기를 몰라도 되는 방법을 찾은 것이죠.

---

## 덧 — alloca는 왜 잘 안 쓰는가

`alloca`가 `malloc`보다 훨씬 빠른데도 실무에서 쓰지 않는 이유를 정리해 둡니다.

| | `alloca` | `malloc` |
|---|---|---|
| 공간 위치 | 현재 함수의 스택 프레임 | 힙 |
| 해제 | 함수 리턴 시 자동 | `free` 필요 |
| 비용 | `%rsp` 조정 — 명령 몇 개 | 빈 블록 탐색, 메타데이터 관리 |
| 할당 실패 | **알려주지 않음** | `NULL` 반환 |
| 쓸 수 있는 크기 | 스택 한도 (리눅스 기본 8MB) | 수 GB |
| 함수 밖으로 넘기기 | 불가 | 가능 |
| 표준 | 비표준 확장 | C 표준 |

1. **실패를 감지할 수 없습니다.** 한도를 넘으면 반환값이 아니라 프로그램이 죽습니다. 사용자 입력 길이를 그대로 넘기는 코드는 3.10.3에서 본 그 취약성이 됩니다.
2. **스택이 작습니다.** 8MB 정도가 기본입니다.
3. **함수 밖으로 못 넘깁니다.** "공간을 만들어 돌려주는 함수"를 만들 수 없습니다.
4. **블록이 아니라 함수 단위로 해제됩니다.** 루프 안에서 부르면 함수가 끝날 때까지 계속 쌓입니다.

C99 이후로는 가변 크기 지역배열이 같은 일을 하면서 **블록을 벗어날 때 해제**되므로 4번 문제가 없습니다. 다만 스택이 터지는 위험은 똑같아서, C11에서는 가변 크기 배열조차 구현 선택 사항으로 내려갔습니다.

# 컴퓨터 시스템 3장 — 3.8 배열 ~ 3.9 이기종 자료구조

> 2편까지가 "계산과 제어"였다면 여기서부터는 **데이터 구조**가 기계 수준에서 어떻게 배치되고 접근되는지를 다룹니다.
> 배열(3.8) → 구조체(3.9.1) → 공용체(3.9.2) → 정렬(3.9.3) 순서이고, 공용체 절은 문장 단위로 정독한 내용을 반영해 자세히 적었습니다.

---

## 목차

- [3.8 배열의 할당과 접근](#38-배열의-할당과-접근)
  - [3.8.1 기본 원리](#381-기본-원리)
  - [3.8.2 포인터 연산](#382-포인터-연산)
  - [3.8.3 다중 배열](#383-다중-배열)
  - [3.8.4 고정 크기 배열](#384-고정-크기-배열)
  - [3.8.5 가변 크기 배열](#385-가변-크기-배열)
- [3.9 이기종 자료구조](#39-이기종-자료구조)
  - [3.9.1 구조체](#391-구조체)
  - [3.9.2 공용체](#392-공용체)
  - [3.9.3 데이터의 정렬](#393-데이터의-정렬)
- [전체 요약](#전체-요약)
- [체크리스트](#체크리스트)

---

# 3.8 배열의 할당과 접근

C의 배열은 구현이 아주 단순해서 기계어 번역이 쉽습니다. 특이한 점은 **원소에 대한 포인터를 만들고 그 포인터끼리 연산할 수 있다**는 것인데, 기계어에서는 전부 주소 계산으로 번역됩니다.

> 다만 최적화 컴파일러는 배열 인덱스의 주소 계산을 단순화하는 데 특히 뛰어나서, C 코드와 기계어의 대응 관계를 읽어내기가 생각보다 까다로워집니다.

## 3.8.1 기본 원리

```c
T A[N];
```

이 선언은 두 가지 일을 합니다.

1. **L · N 바이트의 연속된 공간**을 메모리에 할당 (L은 자료형 T의 크기)
2. 배열 시작 위치를 가리키는 포인터로 쓸 수 있는 **식별자 A**를 도입 (값은 x_A)

원소 i는 주소 **x_A + L · i** 에 있습니다. 전부입니다.

| 선언 | 원소 크기 | 전체 크기 | 시작 주소 | 원소 i의 주소 |
|---|---|---|---|---|
| `char A[12]` | 1 | 12 | x_A | x_A + i |
| `char *B[8]` | 8 | 64 | x_B | x_B + 8i |
| `int C[6]` | 4 | 24 | x_C | x_C + 4i |
| `double *D[5]` | 8 | 40 | x_D | x_D + 8i |

x86-64의 메모리 참조 인스트럭션은 이 계산에 딱 맞게 설계되어 있습니다. E의 주소가 `%rdx`, 인덱스 i가 `%rcx`에 있을 때 `E[i]`(int 배열)를 읽는 코드는 **한 줄**입니다.

```asm
movl (%rdx,%rcx,4), %eax    # M[x_E + 4i]
```

> 💡 배율(scale)로 허용된 값이 **1, 2, 4, 8**인 이유가 여기서 드러납니다. 기본 자료형들의 크기이기 때문입니다.

## 3.8.2 포인터 연산

포인터 연산은 **참조 대상 자료형의 크기만큼 확대**됩니다. p가 자료형 T의 포인터이고 값이 x_p라면, `p+i`는 **x_p + L·i** 입니다.

핵심 등가 관계 두 개:

- `*&Expr` ≡ `Expr`
- `A[i]` ≡ `*(A+i)` — 배열 첨자는 포인터 연산의 문법 설탕일 뿐입니다

E(int 배열)의 주소가 `%rdx`, i가 `%rcx`에 있을 때:

| 수식 | 자료형 | 값 | 어셈블리 |
|---|---|---|---|
| `E` | `int *` | x_E | `movq %rdx, %rax` |
| `E[0]` | `int` | M[x_E] | `movl (%rdx), %eax` |
| `E[i]` | `int` | M[x_E + 4i] | `movl (%rdx,%rcx,4), %eax` |
| `&E[2]` | `int *` | x_E + 8 | `leaq 8(%rdx), %rax` |
| `E+i-1` | `int *` | x_E + 4i − 4 | `leaq -4(%rdx,%rcx,4), %rax` |
| `*(E+i-3)` | `int` | M[x_E + 4i − 12] | `movl -12(%rdx,%rcx,4), %eax` |
| `&E[i]-E` | `long` | i | `movq %rcx, %rax` |

읽어야 할 패턴:

- **값을 가져오는** 연산(`int`) → 4바이트 연산(`movl`) + 32비트 레지스터(`%eax`)
- **포인터를 만드는** 연산(`int *`) → 8바이트 연산(`leaq`) + 64비트 레지스터(`%rax`)
- 마지막 줄 — **두 포인터의 차**는 자료형 크기로 나눈 값이므로 그냥 `i`가 됩니다

## 3.8.3 다중 배열

```c
int A[5][3];
```

는 다음과 정확히 같습니다.

```c
typedef int row3_t[3];
row3_t A[5];
```

즉 **"3개짜리 int 배열"을 원소로 갖는 5개짜리 배열**입니다. 전체 크기는 4 · 5 · 3 = 60바이트.

원소는 **행 우선(row major)** 순서로 저장됩니다. A[0]의 모든 원소 다음에 A[1]의 원소가 오는 식입니다.

| 행 | 원소 | 주소 |
|---|---|---|
| A[0] | A[0][0], A[0][1], A[0][2] | x_A, x_A+4, x_A+8 |
| A[1] | A[1][0], A[1][1], A[1][2] | x_A+12, x_A+16, x_A+20 |
| A[2] | A[2][0], A[2][1], A[2][2] | x_A+24, x_A+28, x_A+32 |
| A[3] | A[3][0], A[3][1], A[3][2] | x_A+36, x_A+40, x_A+44 |
| A[4] | A[4][0], A[4][1], A[4][2] | x_A+48, x_A+52, x_A+56 |

**일반 공식 (식 3.1)** — `T D[R][C];`에서 `D[i][j]`의 주소는

> **&D[i][j] = x_D + L · (C · i + j)**

C는 **열의 개수**, L은 T의 크기입니다. **행의 개수 R은 주소 계산에 등장하지 않습니다.**

5×3 int 배열 A에서 `A[i][j]`를 읽는 코드:

```asm
  A in %rdi, i in %rsi, j in %rdx
leaq (%rsi,%rsi,2), %rax    # 3i
leaq (%rdi,%rax,4), %rax    # x_A + 12i
movl (%rax,%rdx,4), %eax    # M[x_A + 12i + 4j]
```

주소를 x_A + 4(3i + j) 형태로, **곱셈 없이 leaq의 덧셈과 배율만으로** 계산한 것을 보세요.

## 3.8.4 고정 크기 배열

차원이 컴파일 시에 고정되어 있으면 컴파일러는 훨씬 공격적으로 최적화합니다.

```c
/* 원본 */
int fix_prod_ele(fix_matrix A, fix_matrix B, long i, long k) {
    long j; int result = 0;
    for (j = 0; j < N; j++)
        result += A[i][j] * B[j][k];
    return result;
}

/* 컴파일러가 실제로 만든 것을 C로 되돌린 모습 */
int fix_prod_ele_opt(fix_matrix A, fix_matrix B, long i, long k) {
    int *Aptr = &A[i][0];    /* A의 행 i를 따라가는 포인터 */
    int *Bptr = &B[0][k];    /* B의 열 k를 따라가는 포인터 */
    int *Bend = &B[N][k];    /* Bptr의 종료 지점 */
    int result = 0;
    do {
        result += *Aptr * *Bptr;
        Aptr++;              /* 다음 열로 */
        Bptr += N;           /* 다음 행으로 */
    } while (Bptr != Bend);
    return result;
}
```

컴파일러가 한 일:

1. **인덱스 변수 j를 완전히 제거**
2. 모든 배열 참조를 **포인터 역참조**로 변환
3. 루프 종료 조건을 **`Bptr != Bend`** 포인터 비교로 변환

```asm
  A in %rdi, B in %rsi, i in %rdx, k in %rcx
fix_prod_ele:
  salq $6, %rdx             # 64 * i
  addq %rdx, %rdi           # Aptr = x_A + 64i = &A[i][0]
  leaq (%rsi,%rcx,4), %rcx  # Bptr = x_B + 4k = &B[0][k]
  leaq 1024(%rcx), %rsi     # Bend = x_B + 4k + 1024 = &B[N][k]
  movl $0, %eax             # result = 0
.L7:
  movl (%rdi), %edx         # *Aptr
  imull (%rcx), %edx        # × *Bptr
  addl %edx, %eax           # result에 누적
  addq $4, %rdi             # Aptr++      (int 1개)
  addq $64, %rcx            # Bptr += N   (int 16개 = 64바이트)
  cmpq %rsi, %rcx
  jne .L7
  rep; ret
```

> 💡 `Aptr++`이 `addq $4`, `Bptr += N`이 `addq $64`로 나타납니다 — 포인터 연산의 크기 확대가 기계어에서는 **상수 오프셋**으로 굳어집니다. 루프 안에 곱셈은 원래 계산의 `imull` 하나뿐이고 **주소 계산용 곱셈은 전부 사라졌습니다.**

## 3.8.5 가변 크기 배열

역사적으로 C는 컴파일 시 크기가 정해지는 다차원 배열만 지원했고, 프로그래머가 `malloc`으로 직접 할당한 뒤 식 3.1을 손으로 적용해야 했습니다. **ISO C99**부터 배열 차원을 할당 시점에 계산되는 수식으로 쓸 수 있게 되었습니다.

```c
int var_ele(long n, int A[n][n], long i, long j) { return A[i][j]; }
```

> 매개변수 `n`은 반드시 `A[n][n]`보다 **먼저** 와야 합니다. 함수가 매개변수를 만날 때 차원을 계산할 수 있어야 하기 때문입니다.

```asm
  n in %rdi, A in %rsi, i in %rdx, j in %rcx
var_ele:
  imulq %rdx, %rdi          # n · i     ← 곱셈 인스트럭션!
  leaq (%rsi,%rdi,4), %rax  # x_A + 4(n·i)
  movl (%rax,%rcx,4), %eax  # M[x_A + 4(n·i) + 4j]
  ret
```

고정 크기와 **딱 한 군데만** 다릅니다. 고정 크기에서는 `3i`를 `leaq`의 덧셈·배율로 만들 수 있었지만, n이 런타임 값이면 **진짜 곱셈 인스트럭션**을 써야 합니다.

다만 **루프 안에서**는 컴파일러가 접근 패턴의 규칙성을 활용해 다시 최적화합니다.

```asm
  n in %rdi, Arow in %rsi, Bptr in %rcx, 4n in %r9, result in %eax, j in %edx
.L24:
  movl (%rsi,%rdx,4), %r8d  # Arow[j]
  imull (%rcx), %r8d        # × *Bptr
  addl %r8d, %eax
  addq $1, %rdx             # j++
  addq %r9, %rcx            # Bptr += n  ← 미리 계산해둔 4n을 더함
  cmpq %rdi, %rdx           # j:n
  jne .L24
```

`n`(`%rdi`)과 `4n`(`%r9`) **두 값이 모두 필요**한 점을 보세요. 루프 경계 검사에는 n이, 포인터 증가에는 4n이 쓰입니다. 포인터 산술의 배율이 C 코드에서는 보이지 않다가 기계어에서 드러나는 대목입니다.

---

# 3.9 이기종 자료구조

C는 서로 다른 유형의 객체를 묶는 두 가지 방법을 제공합니다.

| | 키워드 | 의미 | 메모리 |
|---|---|---|---|
| **구조체** | `struct` | 여러 객체를 하나의 단위로 **연결** | 각 필드가 **다른** 블록 |
| **공용체** | `union` | 하나의 객체를 여러 자료형으로 **참조** | 모든 필드가 **같은** 블록 |

> 💡 한 줄로 요약하면 **구조체는 "그리고(AND)", 공용체는 "또는(OR)"** 입니다. 이 대비가 3.9절 전체를 관통합니다.

## 3.9.1 구조체

구현은 배열과 비슷합니다 — 모든 컴포넌트가 **메모리의 연속된 영역**에 저장되고, 구조체 포인터는 **첫 바이트의 주소**입니다. 컴파일러는 각 필드의 **바이트 오프셋** 정보를 관리하고, 메모리 참조 인스트럭션의 **변위(displacement)** 로 그 오프셋을 씁니다.

```c
struct rec {
    int i;       /* 오프셋 0  */
    int j;       /* 오프셋 4  */
    int a[2];    /* 오프셋 8  */
    int *p;      /* 오프셋 16 */
};               /* 총 24바이트 */
```

`r`이 `%rdi`에 있을 때:

```asm
/* r->j = r->i */
movl (%rdi), %eax         # r->i  (오프셋 0 → 변위 없음)
movl %eax, 4(%rdi)        # r->j  (오프셋 4)

/* &r->a[i] */
leaq 8(%rdi,%rsi,4), %rax # 8 + 4i

/* r->p = &r->a[r->i + r->j] */
movl 4(%rdi), %eax        # r->j
addl (%rdi), %eax         # + r->i
cltq                      # 8바이트로 확장
leaq 8(%rdi,%rax,4), %rax # &r->a[r->i + r->j]
movq %rax, 16(%rdi)       # r->p에 저장
```

> 💡 **가장 중요한 한 문장**: 구조체에서 필드를 선택하는 일은 **컴파일 시에 완전히 끝납니다.** 기계어 코드에는 필드 선언이나 이름에 대한 정보가 **전혀 없습니다.** 남는 것은 상수 오프셋뿐입니다.

### 오퍼랜드 안에 오프셋과 크기가 함께 있다

`leaq 8(%rdi,%rsi,4), %rax` 한 줄에 3.8과 3.9의 개념이 나란히 들어 있습니다.

```
    변위 (%베이스, %인덱스, 배율)
     ▲                      ▲
   오프셋                  크기
```

- `8` — 구조체 시작에서 배열 `a`까지의 **거리(오프셋)**
- `4` — 그 배열 원소 하나의 **크기(배율)**

두 숫자 모두 단위가 바이트라 헷갈리기 쉽지만, **재는 대상이 다릅니다.**

> **C 초보자 코너 — 구조체로 객체 표현하기**
> `struct`는 C++·자바의 객체에 가장 가까운 C의 기능입니다. 포인터로 다룰 때 `(*rp).width`처럼 괄호가 필요한데, 괄호가 없으면 컴파일러가 `*(rp.width)`로 해석하기 때문입니다. 이 패턴이 워낙 흔해서 C는 `rp->width`라는 축약형을 제공합니다.

---

## 3.9.2 공용체

> 이 절은 문장 단위로 정독한 내용을 반영해 자세히 적었습니다.

### (1) 공용체란 무엇인가

**문법은 구조체와 같고, 의미는 정반대입니다.** 다른 필드들이 메모리의 다른 블록을 참조하는 것이 아니라 **동일한 블록**을 참조합니다.

공용체가 하는 일은 **C의 자료형 체제를 회피해서 하나의 객체가 여러 자료형으로 참조되게** 하는 것입니다.

| | 주소 ↔ 자료형 | 컴파일러의 감시 |
|---|---|---|
| 구조체 | **1:1** — 오프셋 4는 `int`, 끝 | 어긋난 접근을 **막는다** |
| 공용체 | **1:N** — 같은 0번지가 `char`이기도 `int`이기도 | **막을 근거가 없다** |

> 💡 **'회피'의 핵심은 공간이 아니라 감시입니다.**
> 공용체 선언은 컴파일러에게 "이 자리는 문자로 읽어도, 정수로 읽어도, 실수로 읽어도 된다 — 내가 허락한다"고 말한 것입니다. 그러니 실수로 넣고 정수로 꺼내도 **규칙 위반이 아니고**, 에러는커녕 경고도 나오지 않습니다.
> 그물이 찢어진 게 아니라 **선언문으로 그물에 구멍을 내달라고 요청한 것**이고, 그 구멍으로 의도한 것도 실수도 함께 지나갑니다.

컴파일러가 검사하는 것과 못 하는 것:

| | 컴파일러가 보는가 |
|---|---|
| 선언에 어긋나는가 | ○ |
| 의미상 말이 되는가 | ✕ |

### (2) 배치 — 오프셋과 크기

```c
struct S3 { char c; int i[2]; double v; };
union  U3 { char c; int i[2]; double v; };
```

내용물이 완전히 동일하고 키워드 한 글자만 다른 **대조 실험**입니다.

**x86-64 리눅스에서 컴파일하면:**

| Type | c | i | v | 전체 크기 |
|---|---|---|---|---|
| S3 | 0 | 4 | 16 | 24 |
| U3 | 0 | 0 | 0 | 8 |

- 공용체의 **오프셋은 전부 0** — 모두 같은 출발선
- 공용체의 **크기는 가장 큰 멤버만큼** (정렬 때문에 더 커질 수 있으니 정확히는 하한)
- **같은 재료, 같은 문법, 한 글자 차이로 24 대 8**

> ⚠️ **오프셋과 크기는 다른 질문입니다.**
> - 오프셋 = *어디서 시작하는가* (위치)
> - 크기 = *몇 바이트를 쓰는가* (길이)
>
> `double v`의 오프셋은 0이고 크기는 8입니다. 둘 다 맞고 충돌하지 않습니다 — **0번지에서 시작해서 8바이트를 씁니다.**

**바이트로 그려보면:**

```
바이트 번호:   0    1    2    3    4    5    6    7
             ┌────┐
  c          │ c  │                                      ← 0번지에서 1바이트
             └────┘
             ┌───────────────────┐
  i[0]       │      i[0]         │                       ← 0번지에서 4바이트
             └───────────────────┘
             ┌──────────────────────────────────────┐
  v          │              v                       │    ← 0번지에서 8바이트
             └──────────────────────────────────────┘
```

**셋 다 왼쪽 끝(0번지)에서 시작합니다.** 오른쪽으로 얼마나 뻗어나가는지만 다릅니다.

**표 읽기 요령**: 구조체는 오프셋이 단조 증가(0 < 4 < 16), 공용체는 전부 동일. 낯선 코드에서 둘을 구분하는 지문입니다.

### (3) 같은 출발선, 다른 시야

`union U3 *` 타입의 포인터 `p`에 대해 `p->c`, `p->i[0]`, `p->v`는 **모두 자료구조의 시작 부분**을 참조합니다.

| 참조 | 읽는 범위 | 해석 방식 |
|---|---|---|
| `p->c` | 0번지부터 1바이트 | 문자 |
| `p->i[0]` | 0번지부터 4바이트 | 정수 |
| `p->v` | 0번지부터 8바이트 | 부동소수점 |

**출발점은 공유, 읽는 폭과 해석은 제각각.** 주소가 같다는 말이지 읽히는 값이 같다는 말이 아닙니다.

저자가 '첫 번째 필드'가 아니라 **'시작 부분'** 이라 쓴 것도 의미가 있습니다 — 공용체에는 '첫 번째'라는 개념이 무의미합니다. **선언 순서를 바꿔도 표의 숫자는 그대로**입니다. (구조체라면 순서를 바꾸면 오프셋이 전부 바뀝니다.)

> 💡 기계어 관점: 구조체 필드 접근은 베이스 주소에 **변위를 더하는** 인스트럭션이 되지만, 공용체 멤버 접근은 변위가 전부 0이라 **어느 멤버를 썼는지 기계어에 흔적이 남지 않습니다.**

### (4) 경고 — 왜 "치명적"인가

> 공용체는 유용할 수 있지만, C의 자료형 체제가 제공하는 **안전망을 피해가기 때문에 오히려 치명적인 버그를 발생시킬 수도 있습니다.**

이 경고가 **용도 설명이 시작되기 직전**에 놓여 있다는 점이 중요합니다. 유용함을 배우고 남용하지 말라는 순서 배치입니다.

'치명적'인 이유:

- 컴파일 시 오류 없음 → **빌드는 통과**
- 실행해도 즉시 죽지 않음 → **테스트도 통과할 수 있음**
- 엉뚱한 비트를 엉뚱한 타입으로 읽은 값이 **조용히 흘러다님**

즉 **증상이 원인에서 멀리 떨어져** 나타납니다. 잘못 쓴 지점과 이상한 값이 터지는 지점이 코드상·시간상 한참 떨어져 있어 범인을 찾기까지 오래 걸립니다. 타입 오류였다면 컴파일러가 그 줄을 정확히 짚어줬을 텐데, 공용체는 그 감시를 꺼버렸으니까요.

### (5) 용도 ① — 공간 절약

**조건**: 두 필드를 **상호 배타적**으로 쓴다는 것을 **미리 알고 있을 때**.

- *상호 배타적* = 둘 다 동시에 쓰이는 일이 절대 없다
- 공용체는 한쪽에 쓰면 다른 쪽이 덮어써지는데, 애초에 한 번에 하나만 쓸 거라면 **잃을 게 없습니다**
- 컴파일러는 이 조건을 확인해 주지 않습니다. 보증하는 주체는 **프로그래머**뿐이고, **설계 단계에서** 알고 있어야 합니다

**예제: 이진 트리** — 잎새 노드는 값을 갖고, 내부 노드는 두 자식 포인터를 갖습니다.

```c
struct node_s {                    union node_u {
    struct node_s *left;               struct {
    struct node_s *right;                  union node_u *left;
    double data[2];                        union node_u *right;
};  /* 32바이트, 절반 낭비 */            } internal;
                                       double data[2];
                                   };  /* 16바이트 */
```

**두 선언은 실제로 서로 다른 것을 말하고 있습니다.**

| | 선언이 말하는 내용 |
|---|---|
| `struct node_s` | "모든 노드는 left, right, data를 **전부** 갖는다" |
| `union node_u` | "모든 노드는 (left+right) **또는** data 중 하나다" |

프로그램의 실제 의도는 아래쪽입니다. 그렇다면 왜 구조체 버전이 등장할까요?

> **구조체로는 "또는"을 표현할 방법이 없기 때문입니다.**
> 구조체가 할 줄 아는 말은 "전부 갖는다" 하나뿐입니다. "둘 중 하나"를 구조체로 구현하려면 일단 셋 다 담아두고 안 쓰는 쪽을 놔두는 수밖에 없습니다. **낭비는 실수가 아니라 표현력 부족의 대가**입니다.

**낭비의 실체** (64비트 기준):

- 잎새노드 → `data` 16바이트만 쓰고 포인터 자리 16바이트가 놂
- 내부노드 → 포인터 16바이트만 쓰고 `data` 자리 16바이트가 놂
- 어느 쪽이든 **항상 절반이 빕니다**

반면 공용체에서는 같은 16바이트를 노드 종류에 따라 다르게 해석할 뿐이라 **낭비가 0**입니다. (버려지는 게 아니라 **애초에 따로 자리를 주지 않는 것**입니다.)

#### 왜 공용체 쪽에만 구조체가 한 겹 더 있는가

> **구조체는 AND, 공용체는 OR.**

내부 노드는 left와 right를 **동시에** 가져야 합니다 — AND입니다. 그런데 `left`와 `right`를 공용체 멤버로 나란히 적으면 둘 다 오프셋 0이 되어 **겹칩니다.** left에 값을 넣는 순간 right가 날아가고, 자식 둘을 가리켜야 하는데 하나밖에 못 갖게 됩니다.

그래서 **AND로 묶여야 할 것들을 먼저 구조체로 한 덩어리로 만든 뒤**, 그 덩어리 하나를 공용체의 선택지로 넣습니다.

```
union (OR)
├── 선택지 1: struct { left AND right }   ← AND 묶음을 한 덩어리로
└── 선택지 2: data[2]                     ← 혼자서 이미 한 덩어리
```

`data[2]`가 밖에 있는 이유도 여기서 나옵니다 — **묶을 짝이 없기 때문**입니다. 잎새노드 쪽 선택지는 `data` 하나뿐이라 이미 완결된 덩어리입니다.

메모리로 보면:

```
바이트:    0 ─────── 7 │ 8 ─────── 15
internal:  [  left   ] │ [  right  ]     ← 두 자식이 각자 자리를 가짐
data:      [ data[0] ] │ [ data[1] ]     ← 같은 16바이트를 다르게 해석
```

**internal 내부에서는** left와 right가 겹치지 않고(구조체), **internal 전체와 data가** 겹칩니다(공용체). 중첩이 하는 일이 이 **층위 나누기**입니다.

#### `internal`이라는 이름의 정체

C의 선언문은 항상 `[자료형] [이름];` 꼴입니다.

| 자료형 | 이름 |
|---|---|
| `double [2]` | `data` |
| `struct { ... }` (이름 없는 구조체) | `internal` |

`struct { union node_u *left; union node_u *right; }` 전체가 **자료형 자리**이고, `internal`은 그 자료형을 가진 **멤버의 이름**입니다. 구조체를 따로 정의해 두고 쓰는 대신 **정의와 선언을 한 자리에서** 해치운 것이죠.

이름이 필요한 이유는 접근 때문입니다 — `n->internal.left`라고 부르려면 그 덩어리를 부를 말이 있어야 합니다.

#### 접근이 두 단계인 이유

```c
n->internal.left      /* 2층에 있으니 두 걸음 */
n->data[0]            /* 1층에 있으니 한 걸음 + 칸 번호 */
```

| 왼쪽에 있는 것 | 쓰는 기호 |
|---|---|
| 주소(포인터) | `->` |
| 값(실물) | `.` |

`n`은 포인터(`union node_u *`)이므로 첫 걸음은 `->`. 그 결과 손에 쥔 `internal`은 포인터가 아니라 **구조체 실물**이므로 두 번째 걸음은 `.`입니다.

> 💡 접근식은 두 단계지만 **기계어는 한 번의 덧셈**입니다. `n->internal`은 오프셋 0, 그 안에서 `left`도 0 → 합쳐서 0. `right`는 0 + 8 = 8. 컴파일 시점에 다 계산됩니다.

#### 왜 하필 `double data[2]`인가

```
선택지 A (internal):  8 + 8 = 16바이트
선택지 B (data[2]):   8 × 2 = 16바이트
                      ────────────────
공용체 크기 = 16바이트, 어느 쪽으로 쓰든 낭비 0
```

우연이 아니라 **의도적인 설계**입니다. 만약 `data[1]`(8바이트)이었다면 공용체 크기는 여전히 16이고(더 큰 쪽이 정하므로) 잎새노드일 때 8바이트가 놀게 됩니다. **선택지들의 크기를 비슷하게 맞추는 것**이 공용체 설계의 실전 요령입니다.

> 그리고 `data`를 아예 빼면 낭비는 없어지지만 **잎새노드가 아무것도 담지 못합니다.** 짐이 무겁다고 짐을 버리는 셈이죠 — `data`는 낭비의 원인이 아니라 트리의 존재 이유입니다.

### (6) 잃어버린 정보와 태그 필드

> 이런 인코딩 방식을 사용하면 **주어진 노드가 잎새노드인지 내부 노드인지 구분할 수 없습니다.**

16바이트를 아꼈는데, 그 16바이트 안의 비트가 포인터 두 개인지 실수 두 개인지 알 방법이 사라졌습니다.

**원인은 크기가 아니라 오프셋입니다.** 두 선택지의 크기가 달랐어도 둘 다 0번지에서 시작하니 여전히 겹치고, 여전히 구분할 수 없습니다.

**그리고 이 "모른다"는 실행 시점의 문제입니다.**

| | 언제 | 무엇을 모르나 | 누가 |
|---|---|---|---|
| 앞의 경고 | 컴파일 시점 | 이 접근이 실수인지 의도인지 | 컴파일러 |
| 이 문장 | **실행 시점** | 이 노드가 지금 잎새인지 내부인지 | **프로그램 자신** |

구조체였다면 이 문제가 없었습니다 — 낭비했던 16바이트가 사실은 **구분 가능성을 사 주고 있었던 것**입니다. **공간을 얻고 정보를 잃은** 셈이죠.

#### 표준 해법 — 열거형 태그 + 공용체를 구조체로 감싸기

```c
typedef enum { N_LEAF, N_INTERNAL } nodetype_t;
struct node_t {
    nodetype_t type;
    union {
        struct { struct node_t *left; struct node_t *right; } internal;
        double data[2];
    } info;
};  /* 24바이트 */
```

이것은 저자가 발명한 요령이 아니라 **이 문제의 표준 해법**입니다. 공용체를 쓰는 곳이면 거의 어디서나 이 모양이 나옵니다.

**논리는 단순합니다** — 잃어버린 것이 "지금 어느 쪽인가"라는 정보였으니, **그 정보를 적어둘 칸을 하나 만든다.**

**왜 열거형인가**: 그냥 0과 1을 써도 동작은 합니다. 하지만 코드에 숫자만 굴러다니면 읽는 사람이 뜻을 모릅니다. 열거형은 그 숫자들에 `N_LEAF`, `N_INTERNAL`이라는 **이름표**를 붙이고, 선택지가 몇 가지인지도 선언문 한 줄에 드러냅니다.

**구조가 뒤집혔다는 점에 주목:**

```
struct node_t          ← AND: type 그리고 info를 둘 다 갖는다
├── type               (지금 어느 쪽인지)
└── info               ← OR: internal 또는 data 중 하나
    ├── internal
    └── data
```

태그와 내용물은 **항상 함께** 있어야 하니 AND → 구조체. 내용물 안쪽은 여전히 둘 중 하나이니 OR → 공용체. **AND가 OR을 감싸는 2층 구조**입니다.

> 💡 앞의 `node_u`에서는 **OR이 AND를 감쌌습니다**(공용체 안에 구조체). 같은 두 키워드로 어느 쪽을 바깥에 두느냐에 따라 전혀 다른 것이 만들어집니다.

#### 비용 계산

> 이 구조체는 총 **24바이트**를 필요로 합니다: 4바이트는 type, 8바이트씩을 `info.internal.left`와 `info.internal.right`에, **또는** 16바이트를 `info.data`에.

내역에 **'또는'이 섞여 있는 점**에 주의하세요. 보통 내역은 전부 더하면 총액이 되는데, 공용체 부분은 택일이라 더하면 안 됩니다.

그런데 4 + 16 = 20인데 본문은 24라고 합니다.

```
바이트:  0 ─ 3 │ 4 ─ 7 │ 8 ──────────── 23
        [type ][ ???? ][      info      ]
                  ▲
            정체는 3.9.3에서
```

`info` 안에 8바이트짜리들이 있어서 `info`는 아무 자리에나 놓일 수 없고, `type`이 4바이트를 쓴 뒤 **빈 4바이트가 삽입된 뒤에** 시작합니다. S3에서 "i의 오프셋이 왜 4인지"를 미뤘던 것과 같은 빚이고, 다음 절에서 한꺼번에 갚습니다.

#### 세 설계의 성적표 (64비트)

| 설계 | 크기 | 구분 가능 | 코드 복잡도 |
|---|---|---|---|
| 구조체만 | 32 | ○ | 단순 |
| 순수 공용체 | 16 | **✕** | 중간 |
| 태그 + 공용체 | 24 | ○ | 복잡 |

> ⚠️ **순수 공용체 16바이트는 실은 선택지가 아닙니다.** 노드 종류를 구분할 수 없으면 트리가 제대로 동작하지 않으니까요. 실제로 고를 수 있는 안은 32와 24 둘뿐이고, **실질 절약분은 8바이트**입니다.
> (단, 노드 종류를 다른 경로로 알 수 있다면 — 부모가 이미 알고 있는 구조라면 — 태그 없이도 됩니다. "구분할 수 없다"는 **이 노드만 손에 들고 있을 때**의 이야기입니다.)

#### 저자의 평가

> 이 경우엔 코드가 복잡해지는 대가에 비해 **이득이 크지 않습니다**(32 → 24바이트). 필드가 더 많은 자료구조에서 의미가 커집니다.

**"크지 않다"의 근거**: 절약은 8바이트(25%)인데, 대가로

- 태그를 **항상 올바르게 유지할 책임**이 프로그래머에게 생김
- 접근 경로가 `n->info.internal.left`로 길어짐
- 노드를 읽는 코드마다 **태그를 먼저 확인하는 분기**가 붙음

**"필드가 많아지면 달라지는" 이유**: 비용은 고정이고 이득만 자랍니다.

```
지금 예제:      선택지 16바이트 × 2가지 → 절약 16, 태그 8 → 순이득 8
필드가 많다면:  선택지 100바이트 × 2가지 → 절약 100, 태그 8 → 순이득 92
선택지가 3가지: 안 쓰는 두 쪽을 모두 아낌 → 이득이 더 커짐
```

태그는 선택지가 2개든 10개든 4바이트 하나면 됩니다. 분모는 그대로인데 분자만 자라니, 자료구조가 커질수록 **번거로움을 감수할 값어치**가 생깁니다.

### (7) 용도 ② — 비트 패턴 접근

**여기서 전제가 정반대로 뒤집힙니다.**

| | 용도 ① 공간 절약 | 용도 ② 비트 패턴 접근 |
|---|---|---|
| 멤버들의 관계 | **상호 배타적** — 한 번에 하나만 | **같은 데이터** — 일부러 양쪽으로 본다 |
| 겹침은 | 감수하는 **부작용** | 노리는 **목적** |
| 중첩 구조 | 필요 (AND 묶기) | 불필요 |

용도 ①에서는 "겹치니까 하나만 쓰세요"였는데, 용도 ②에서는 **"겹치니까 한쪽으로 넣고 다른 쪽으로 꺼내세요"** 가 됩니다. 그래서 "공용체 = 배타적인 것들에 쓴다"로 굳히면 안 됩니다.

#### 먼저 비교 대상 — 형변환

```c
unsigned long u = (unsigned long) d;
```

`d`가 1.0일 때:

```
d = 1.0  (double, 8바이트)
비트:  0011111111110000000000000000...0000
       └부호┘└─ 지수 ─┘└──── 유효숫자 ────┘

           ↓  (unsigned long) 형변환
           ↓  "1.0이라는 값을 정수로 표현하면 1이다"
           ↓  → 정수 1을 새로 만들어 넣음

u = 1    (unsigned long, 8바이트)
비트:  0000000000000000000000000000...0001
```

**값은 같은데 비트는 완전히 딴판입니다.** 형변환은 비트를 옮기는 것이 아니라 **"같은 값을 새 자료형의 방식으로 다시 표현"** 합니다. 원래 비트는 버려집니다. (3.14였다면 소수점 이하를 버리고 3이 됩니다 — 값도 정확히 보존되지는 않고 가능한 한 가깝게 옮기는 것입니다.)

**'d가 0.0인 경우를 제외하고'라는 단서의 정체:**

```
double 0.0 의 비트: 0000 0000 ... 0000  (전부 0)
정수 0     의 비트: 0000 0000 ... 0000  (전부 0)
```

**0.0만 유일하게 양쪽 비트가 같습니다.** 우연히 겹친 특수 경우라 예외로 빼둔 것이고, "나머지 경우엔 정말 다르다"를 강조하는 장치이기도 합니다.

#### 공용체 버전

```c
unsigned long double2bits(double d) {
    union {
        double d;
        unsigned long u;
    } temp;
    temp.d = d;
    return temp.u;     /* d와 완전히 동일한 비트 표현 */
}
```

**한 문장으로: 한쪽 이름으로 넣고, 다른 쪽 이름으로 꺼낸다.**

```
temp.d = d;      ← double이라는 이름으로 8바이트를 채운다
return temp.u;   ← 같은 8바이트를 unsigned long이라는 이름으로 읽는다
```

그 사이에 **아무 일도 일어나지 않습니다.** 변환도, 계산도, 복사도 없습니다. 비트는 놓인 그대로 있고 **부르는 이름만 바뀝니다.**

가능한 이유는 `temp.d`와 `temp.u`의 오프셋이 둘 다 0이기 때문입니다 — 물리적으로 **같은 8바이트**입니다. "모든 멤버가 같은 출발선"이 여기서 도구로 쓰입니다.

> ⚠️ **두 멤버의 크기가 같아야 합니다** — `double`도 8바이트, `unsigned long`도 8바이트. 어긋나면 일부만 읽히거나 남은 자리에 쓰레기가 딸려옵니다. 이 예제가 굳이 `unsigned long`을 고른 이유입니다.

**두 방식을 나란히** (`d = 1.0`):

| | 지키는 것 | 버리는 것 | 결과 `u` |
|---|---|---|---|
| `(unsigned long) d` | **값** | 비트 | `1` |
| `double2bits(d)` | **비트** | 값 | `4607182418800017408` |

두 번째 숫자는 **1.0의 비트를 정수인 척 읽은 결과**입니다. 숫자로서는 의미가 없고, **비트를 들여다보기 위한 창**일 뿐입니다. 그래서 본문도 "u의 숫자 값은 d가 0.0인 경우 외에는 d의 값과 연관성이 없다"고 못박습니다 — **값으로 읽지 말라**는 경고입니다.

`u` 안에 들어 있는 것은 `double`의 세 구역, 즉 **부호비트 1 + 지수 11 + 유효숫자 52비트**입니다. 이 구조를 제대로 다루는 것이 3.11절입니다.

> 💡 **이것이 실무에서 더 자주 보는 용도입니다.** 부동소수점이 왜 이상하게 동작하는지 디버깅하거나, 값을 파일·네트워크로 그대로 내보내거나, 비트를 직접 조작할 때 씁니다. 그리고 C에서 이 일을 하는 **표준적이고 관용적인 방법**이 이 패턴입니다.

#### 왜 하필 공용체인가 — 다른 선택지들

`double d = 1.0;`이라고 선언한 순간 `d`라는 이름은 "이 8바이트를 실수로 해석하라"는 명령과 한 몸이 됩니다. 비트를 보려면 **정수의 눈**이 필요한데, 그 눈을 같은 메모리에 붙이는 방법이 많지 않습니다.

| 방법 | 되는가 |
|---|---|
| 형변환 | ✕ 값을 새로 만드니 원본 비트가 사라짐 |
| 포인터 캐스팅 | △ 비트는 보존되지만 strict aliasing 위반 → UB |
| `memcpy` | ○ 합법이고 안전 |
| **공용체** | ○ 합법이고 읽기 쉬움 |

```
비트를 한 번만 꺼내 본다        →  memcpy
같은 메모리를 두 눈으로 계속 쓴다  →  공용체
```

**공용체만 되는 것**: 하드웨어 레지스터를 비트필드 구조체와 32비트 정수로 **동시에** 보는 관용구. 설정을 읽을 때는 이름으로, 레지스터에 쓸 때는 통째로. `memcpy`는 복사본이 생기는 순간 "같은 것"이 아니게 되어 이 일을 못 합니다.

### (8) 바이트 순서 — 엔디안

> 서로 다른 크기의 자료형들을 한데 묶기 위해 공용체를 사용할 때는 **바이트 순서 문제**가 중요해질 수 있습니다.

```c
double uu2double(unsigned word0, unsigned word1) {
    union { double d; unsigned u[2]; } temp;
    temp.u[0] = word0;
    temp.u[1] = word1;
    return temp.d;
}
```

**앞 예제와 무엇이 달라졌는가:**

| | `double2bits` | `uu2double` |
|---|---|---|
| 방향 | double → 비트 (**읽기**) | 비트 → double (**조립**) |
| 짝 | 8바이트 ↔ 8바이트 | 8바이트 ↔ **4 + 4** |

문제를 일으키는 것은 두 번째입니다. **8바이트를 두 조각으로 쪼개는 순간** "어느 조각이 앞인가"라는 질문이 생깁니다. 쪼개지 않았던 앞 예제에는 없던 질문입니다.

**헷갈리지 말아야 할 것**: `u[0]`은 **항상 낮은 주소 쪽 4바이트**입니다. 기계와 무관하게 고정입니다. 엔디안이 바꾸는 것은 **그 "앞쪽 주소"가 수의 어느 부분이냐**입니다.

```
메모리 주소:        낮음 ──────────→ 높음
                  [   u[0]   ][   u[1]   ]

리틀 엔디안(x86-64):  하위 4바이트   상위 4바이트
빅 엔디안:            상위 4바이트   하위 4바이트
```

**같은 코드, 같은 입력, 다른 결과** → 이식성 문제입니다.

**엔디안 자체를 정리하면:**

```
0x12345678 을 0x100번지부터 저장하면

주소:          0x100   0x101   0x102   0x103
빅 엔디안:      12      34      56      78     ← 큰 자리부터
리틀 엔디안:    78      56      34      12     ← 작은 자리부터
```

- **리틀 엔디안** — x86, x86-64, ARM(기본). 낮은 주소에 낮은 자리
- **빅 엔디안** — 메인프레임, SPARC, **네트워크 프로토콜 표준**. 낮은 주소에 높은 자리
- 이름은 『걸리버 여행기』의 달걀 깨는 쪽 논쟁에서. **본질적이지 않은 차이로 싸운다**는 야유
- 엔디안은 **자동**입니다 — CPU가 메모리를 읽고 쓰는 방식 그 자체라 켜고 끄는 게 아닙니다
- 필요할 때는 CPU를 바꾸는 게 아니라 **데이터를 뒤집습니다** (`htonl` 계열, `<endian.h>`, `__builtin_bswap*`)

**언제 드러나는가:**

| 상황 | 예 |
|---|---|
| 바이트 단위로 쪼개 볼 때 | **`uu2double`이 이 경우** |
| 다른 기계로 보낼 때 | 네트워크 통신 |
| 파일로 저장하고 읽을 때 | 바이너리 파일 포맷 |

공통점은 **자료형이라는 포장지를 벗기고 메모리를 날것으로 다룬다**는 것입니다. 공용체가 하는 일이 바로 그 포장지를 벗기는 것이니, 이 절 끝에 엔디안이 등장하는 게 자연스럽습니다.

> 💡 **배열·문자열에는 엔디안이 적용되지 않습니다.** 원소 하나가 1바이트라 쪼갤 일이 없기 때문입니다. 네트워크 프로토콜에 텍스트 기반이 많은 이유 중 하나입니다.
> 반대로 `int arr[2]`라면 **원소 사이의 순서는 그대로지만 각 원소 안에서는 바이트가 뒤집힙니다.**

### (9) 공용체 절 정리

| 질문 | 답 |
|---|---|
| 공용체의 오프셋은? | **전부 0** (플랫폼 무관, C가 보장) |
| 공용체의 크기는? | **가장 큰 멤버만큼** (정렬 때문에 더 커질 수 있음, 플랫폼 영향 받음) |
| 용도는 몇 가지? | **둘** — ① 공간 절약 ② 비트 패턴 접근 |
| 용도 ①의 조건은? | **상호 배타적**이라는 것을 미리 알고 있을 것 |
| 무엇을 잃는가? | 컴파일러의 감시, 그리고 "지금 어느 쪽인가"라는 정보 |
| 되사오는 방법은? | **태그 필드** (열거형 + 공용체를 구조체로 감싸기) |
| 형변환과의 차이는? | 형변환은 **값** 보존, 공용체는 **비트** 보존 |
| 언제 엔디안이 문제되나? | 큰 값을 **여러 조각으로 쪼갤 때** |

---

## 3.9.3 데이터의 정렬

많은 시스템은 객체의 주소가 어떤 값 K(보통 2, 4, 8)의 배수일 것을 요구하거나 권장합니다. 이유는 **하드웨어 설계 단순화**입니다.

프로세서가 항상 8의 배수 주소에서 8바이트씩 인출한다면, 모든 `double`이 8의 배수에 정렬되어 있을 때 **한 번의 메모리 연산**으로 읽을 수 있습니다. 정렬되지 않으면 두 블록에 걸쳐 있을 수 있어 **두 번 접근**해야 합니다.

> x86-64 **하드웨어는 정렬과 무관하게 정확히 동작합니다.** 정렬은 정확성이 아니라 **성능**을 위한 인텔의 권장 사항입니다. (아래 SSE 예외 제외)

**정렬 규칙: K바이트 원시 객체의 주소는 K의 배수여야 한다.**

| K | 자료형 |
|---|---|
| 1 | `char` |
| 2 | `short` |
| 4 | `int`, `float` |
| 8 | `long`, `double`, 모든 포인터 |

컴파일러는 어셈블리에 `.align 8` 같은 **디렉티브**를 삽입해 전역 데이터의 정렬을 보장합니다.

### 패딩 ① — 필드 사이

```c
struct S1 { int i; char c; int j; };
```

최소 크기 9바이트로 배치하면 i는 오프셋 0(OK)인데 j가 오프셋 5가 되어 **4바이트 정렬을 만족하지 못합니다.** 그래서 컴파일러는 c와 j 사이에 **3바이트를 삽입**합니다.

```
 오프셋:  0        4    5           8        12
        ┌────────┬────┬───────────┬────────┐
        │   i    │ c  │  (패딩 3) │   j    │
        └────────┴────┴───────────┴────────┘
        전체 크기 12바이트
```

덤으로, 컴파일러는 모든 `struct S1 *` 포인터가 **4바이트 정렬**을 만족하도록 보장합니다. x_p가 4의 배수면 `p->i`(x_p)와 `p->j`(x_p+8)가 모두 자동으로 정렬됩니다.

### 패딩 ② — 구조체 끝

```c
struct S2 { int i; int j; char c; };   /* 9바이트면 충분해 보이지만... */
struct S2 d[4];
```

9바이트로 잡으면 배열 원소가 x_d, x_d+9, x_d+18, x_d+27에 놓여 **정렬이 깨집니다.** 그래서 컴파일러는 구조체 **끝에 3바이트를 더 붙여 12바이트**로 만듭니다. 그러면 원소들은 x_d, x_d+12, x_d+24, x_d+36에 놓이고 x_d만 4의 배수면 모두 만족합니다.

> 💡 **패딩은 두 군데에서 생깁니다** — 필드 사이(각 필드의 정렬을 맞추려고), 그리고 구조체 끝(배열로 만들었을 때를 대비해서). 구조체 전체의 정렬 요건은 **가장 큰 정렬 요건을 가진 필드**를 따릅니다. 그래서 **필드를 큰 것부터 나열하면 낭비 공간이 줄어듭니다.**

### 미뤄뒀던 빚 갚기

이제 3.9.2에서 넘어갔던 두 숫자를 설명할 수 있습니다.

**① `S3`에서 `i`의 오프셋이 왜 4인가, `v`가 왜 16인가**

```c
struct S3 { char c; int i[2]; double v; };
```

- `c` → 오프셋 0, 1바이트 사용. 다음 빈자리는 1번지
- `i[2]` → `int`는 4의 배수 주소를 요구. 1번지는 안 되므로 **4번지로 밀림.** 8바이트 사용(4~11). 다음 빈자리는 12번지
- `v` → `double`은 8의 배수 주소를 요구. 12는 8의 배수가 아니므로 **16번지로 밀림.** 8바이트 사용(16~23)
- 전체 24바이트

```
64비트:  [c][× × ×][   i[0]   ][   i[1]   ][× × × ×][        v        ]
          0  1 2 3   4       7   8      11  12   15   16           23    → 24바이트
```

**② `node_t`가 왜 20이 아니라 24인가**

`type`(4) + `info`(16) = 20인데 실제는 24. `info` 안에 8바이트짜리 멤버들이 있어 `info`의 정렬 요구는 8입니다. `type`이 0~3번지를 쓰고 나면 다음은 4번지인데 8의 배수가 아니므로 **4바이트 패딩 후 8번지부터** `info`가 시작합니다.

```
바이트:  0 ─ 3 │ 4 ─ 7 │ 8 ──────────── 23
        [type ][ 패딩 ][      info      ]      → 24바이트
```

### 플랫폼에 따른 변화

정렬 규칙은 **ABI(CPU + OS + 컴파일러의 약속)** 에 속하므로 플랫폼마다 다를 수 있습니다.

| 자료형 | 32비트 리눅스 정렬 | 64비트 리눅스 정렬 |
|---|---|---|
| `char` | 1 | 1 |
| `short` | 2 | 2 |
| `int`, `float` | 4 | 4 |
| **`double`** | **4** | **8** |
| **`long long`** | **4** | **8** |
| 포인터 | 4 | 8 |

그 결과 같은 선언이 다른 표를 냅니다.

```
S3:  64비트 → c=0, i=4, v=16 → 24바이트
     32비트 → c=0, i=4, v=12 → 20바이트
```

> ⚠️ **`double`의 크기는 양쪽 다 8바이트로 똑같습니다.** 바뀐 것은 크기가 아니라 **"어디에 놓일 수 있는가"라는 정렬 규칙**이고, 그것이 오프셋을 바꾸고 오프셋이 전체 크기를 바꿉니다. **크기와 오프셋이 독립적인 두 축**이라는 증거입니다.

주요 자료형 크기 비교:

| 자료형 | 32비트 | 64비트 | 변하나 |
|---|---|---|---|
| `char` / `short` / `int` / `float` / `double` / `long long` | 1/2/4/4/8/8 | 동일 | |
| **`long`** | **4** | **8** | ✔ |
| **포인터** | **4** | **8** | ✔ |
| **`long double`** | **12** | **16** | ✔ |

> 💡 같은 64비트 CPU라도 **윈도우에서는 `long`이 4바이트**입니다. 저자가 "x86-64"만 쓰지 않고 "**x86-64 리눅스**"라고 OS까지 명시한 이유입니다. 그래서 C에는 `sizeof` 연산자가 있습니다 — 크기를 숫자로 적어 넣지 말고 그때그때 물어보라는 것이죠.

### 추가 정보 — 정렬이 "강제"되는 사례

대부분의 x86-64 인스트럭션에서 정렬은 효율성 문제일 뿐이지만, 일부 프로세서 모델은 멀티미디어용 **SSE 인스트럭션**에서 정렬되지 않은 데이터에 대해 정확히 동작하지 않습니다. 이 인스트럭션들은 16바이트 블록을 다루며 주소가 **16의 배수**여야 하고, 어기면 예외가 발생해 보통 프로그램이 종료됩니다.

그래서 x86-64용 컴파일러와 런타임은 두 가지를 보장합니다.

- `alloca`, `malloc`, `calloc`, `realloc`이 반환하는 모든 블록의 시작 주소는 **16의 배수**
- 대부분 함수의 **스택 프레임은 16바이트 경계로 정렬**

> (3.7.5절의 `P` 함수에서 `subq $8, %rsp`가 아무 변수도 없이 등장했던 것 — 그게 이 16바이트 정렬을 맞추려는 코드였습니다.)
> 더 최신 x86-64의 **AVX** 인스트럭션에는 이런 강제 정렬 요구사항이 없습니다.

---

# 전체 요약

## 3.8 → 3.9로 이어지는 하나의 이야기

세 절이 전부 **"이름을 주소로 바꾸는 법"** 을 다룹니다.

| 절 | 이름 → 주소 공식 |
|---|---|
| 3.8 배열 | `A[i]` → x_A + **L · i** (크기 × 인덱스) |
| 3.9.1 구조체 | `r->f` → x_r + **오프셋** (컴파일 시 결정된 상수) |
| 3.9.2 공용체 | `u.f` → x_u + **0** (전부 같은 자리) |
| 3.9.3 정렬 | 그 오프셋이 **어떻게 계산되는지**의 나머지 절반 |

그리고 메모리 참조 오퍼랜드 하나에 두 개념이 나란히 들어 있습니다.

```
    변위 (%베이스, %인덱스, 배율)
     ▲                      ▲
   오프셋 (3.9)           크기 (3.8)
```

## 세 가지 핵심 대비

**① 구조체 AND / 공용체 OR**

| | 표현하는 관계 | 오프셋 | 크기 |
|---|---|---|---|
| 구조체 | 전부 갖는다 (AND) | 각자 다름, 단조 증가 | 합 + 패딩 |
| 공용체 | 둘 중 하나 (OR) | **전부 0** | **최댓값** (+ 정렬) |

중첩하면 어느 쪽을 바깥에 두느냐에 따라 전혀 다른 것이 됩니다 — `node_u`는 OR이 AND를 감싸고, `node_t`는 AND가 OR을 감쌉니다.

**② 오프셋(위치) / 크기(길이)**

| | 재는 대상 | 무엇이 정하나 | 플랫폼 민감도 |
|---|---|---|---|
| 크기 | 그 멤버 자신 | 자료형(× 개수) | 일부만 (포인터·long·long double) |
| 오프셋 | 시작점과의 거리 | 앞선 멤버 크기 + 정렬 패딩 | **더 높음** (정렬 규칙만 변해도) |

**③ 값 보존 / 비트 보존**

| | 지키는 것 | 버리는 것 |
|---|---|---|
| 형변환 | 값 | 비트 |
| 공용체 | 비트 | 값 |

## 기계어가 남기는 것과 지우는 것

- **지우는 것**: 필드 이름, 자료형 이름, 배열이라는 사실
- **남는 것**: 상수 오프셋(변위)과 원소 크기(배율)뿐

그래서 3.9.1의 결론 — "구조체에서 다른 필드를 선택하는 것은 **컴파일 시에 완벽하게 처리된다**" — 가 공용체에서는 한층 더 극단으로 갑니다. 오프셋이 전부 0이라 **어느 멤버를 썼는지 기계어에 흔적조차 남지 않습니다.**

---

# 체크리스트

**3.8 배열**

- `T D[R][C]`에서 `D[i][j]`의 주소 공식은? 이 공식에 **R이 없는** 이유는?
- 허용되는 배율이 1, 2, 4, 8인 이유는?
- 고정 크기 배열 루프에서 컴파일러가 하는 세 가지 최적화는?
- 가변 크기 배열에서 `leaq` 대신 `imulq`가 나오는 이유는?
- 루프에서 `n`과 `4n` 두 값이 모두 필요한 이유는?

**3.9.1 구조체**

- 기계어 코드에 구조체 필드 **이름**이 남는가?
- 필드 오프셋은 기계어의 어느 자리에 나타나는가?
- `8(%rdi,%rsi,4)`에서 `8`과 `4`는 각각 무엇인가?

**3.9.2 공용체**

- 공용체의 오프셋은? 플랫폼에 따라 변하는가?
- 공용체의 전체 크기를 결정하는 것은?
- "자료형 체제를 회피한다"에서 실제로 잃는 것은 무엇인가? (공간? 감시?)
- 용도 두 가지는? 각각의 전제가 어떻게 다른가?
- `node_u`에서 `internal`을 구조체로 한 번 더 감싼 이유는?
- `node_u`와 `node_t`의 중첩 구조는 어떻게 다른가?
- "노드가 잎새인지 내부인지 구분할 수 없다"의 원인은 크기인가 오프셋인가?
- 태그 필드로 되사오는 정보는 무엇이고 비용은 얼마인가?
- 형변환 `(unsigned long) d`와 공용체를 통한 재해석의 차이는?
- `d`가 0.0일 때만 예외가 생기는 이유는?
- `double2bits`에는 없고 `uu2double`에만 있는 문제는 무엇이고 왜 그런가?
- 비트 패턴 접근에도 태그 필드가 필요한가?

**3.9.3 정렬**

- 정렬이 요구되는 이유는? x86-64에서 정렬은 정확성 문제인가 성능 문제인가?
- 패딩이 삽입되는 두 위치는?
- 낭비 공간을 줄이려면 필드를 어떤 순서로 나열해야 하는가?
- `S3`에서 `v`의 오프셋이 64비트에서 16, 32비트에서 12인 이유는? (`double`의 크기는 양쪽 다 8인데)
- `node_t`가 20이 아니라 24바이트인 이유는?
- `malloc` 계열이 반환하는 주소가 16의 배수인 이유는?
